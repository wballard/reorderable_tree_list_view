name: example
description: Example app for ReorderableTreeListView
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  reorderable_tree_list_view:
    path: ../
  storybook_flutter: ^0.14.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

# Web deployment configuration
# To build for web: flutter build web --web-renderer html
# Deploy to GitHub Pages by copying build/web/* to gh-pages branch